# 011-auth-mock UI 功能测试用例

> 模块: 011-auth-mock
> 对应规格: [spec.md](./spec.md)
> 对应方案: [plan.md](./plan.md)
> 状态: 已实现
> 最后更新: 2026-07-19

## 1. 登录成功（模拟认证）

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 关联需求 | 优先级 |
|------|---------|---------|---------|---------|---------|--------|
| TC-011-001 | 后端不可达时使用模拟认证成功登录 | 后端 `/authcenter/login` 不可达（网络错误）；`public/USERS_MOCK.json` 可正常 fetch；`window.SYSTEM_CONFIGS.rsaPublicKey` 已配置 | 1. 刷新页面，确认登录对话框出现<br>2. 输入用户名 `admin`<br>3. 输入密码 `admin123`<br>4. 点击"登录"按钮或按 Enter | 1. 登录按钮显示"登录中…"加载状态<br>2. 控制台有 `[auth-service] 认证中心不可用，降级到 USERS_MOCK.json 模拟登录` 警告日志<br>3. 登录对话框消失，桌面正常渲染（状态栏 + 视口 + 工具栏）<br>4. 状态栏显示"管理员"<br>5. localStorage 中存在 `dashboard-login-user` = `{"userId":"1001","userName":"admin","name":"管理员"}`（不含 password） | FR-011-001, FR-011-008, FR-011-011, FR-011-012, FR-011-013 | P0 |
| TC-011-002 | 刷新页面后自动恢复登录会话 | 已通过 TC-011-001 登录成功，localStorage 中有有效的 `dashboard-login-user` | 1. 刷新浏览器页面（F5 或 Ctrl+R） | 1. 登录对话框不出现<br>2. 桌面直接渲染<br>3. 状态栏显示"管理员"<br>4. 桌面布局与刷新前一致 | FR-011-015, FR-011-016 | P0 |
| TC-011-003 | RSA 公钥配置有效时登录 | 后端不可达；`window.SYSTEM_CONFIGS.rsaPublicKey` 为有效 PEM 格式公钥 | 1. 输入有效用户名密码<br>2. 在浏览器 DevTools Network 面板观察请求 | 1. 登录成功<br>2. Network 面板可见 POST 请求的 body 中 password 字段为 base64 密文（非明文）<br>3. 登录流程完成 | FR-011-004 | P1 |

## 2. 登录失败

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 关联需求 | 优先级 |
|------|---------|---------|---------|---------|---------|--------|
| TC-011-004 | 后端不可达 + 模拟认证错误密码 | 后端不可达；`public/USERS_MOCK.json` 可正常 fetch | 1. 输入用户名 `admin`<br>2. 输入密码 `wrongpassword`<br>3. 点击"登录" | 1. 登录按钮恢复为"登录"<br>2. 对话框内显示红色错误提示"用户名或密码错误"<br>3. 密码输入框内容保留（不清空）<br>4. localStorage 中不存在 `dashboard-login-user` | FR-011-009, FR-011-008 | P0 |
| TC-011-005 | 后端不可达 + 模拟认证不存在的用户名 | 后端不可达；`public/USERS_MOCK.json` 可正常 fetch | 1. 输入用户名 `nonexistent`<br>2. 输入任意密码<br>3. 点击"登录" | 对话框显示"用户名或密码错误" | FR-011-009 | P1 |
| TC-011-006 | 空用户名提交 | 登录对话框显示中 | 1. 用户名字段留空<br>2. 密码填入任意值<br>3. 点击"登录" | 对话框显示"请输入用户名和密码"，不发起网络请求 | FR-011-002 | P0 |
| TC-011-007 | 空密码提交 | 登录对话框显示中 | 1. 用户名填入有效值<br>2. 密码字段留空<br>3. 点击"登录" | 对话框显示"请输入用户名和密码"，不发起网络请求 | FR-011-002 | P0 |
| TC-011-008 | 两字段均为空提交 | 登录对话框显示中 | 1. 用户名和密码均留空<br>2. 点击"登录" | 对话框显示"请输入用户名和密码" | FR-011-002 | P1 |
| TC-011-009 | 模拟用户数据加载失败 | 后端不可达，且 `/USERS_MOCK.json` 返回 404 或网络错误 | 1. 删除或重命名 `public/USERS_MOCK.json`<br>2. 输入任意用户名密码<br>3. 点击"登录" | 对话框显示"登录服务不可用，且模拟用户数据加载失败" | NFR-011-006 | P1 |
| TC-011-010 | RSA 公钥缺失时登录 | `window.SYSTEM_CONFIGS.rsaPublicKey` 为 undefined 或空字符串 | 1. 修改 `public/config.js` 删除 rsaPublicKey<br>2. 刷新页面<br>3. 输入有效用户名密码<br>4. 点击"登录" | 对话框显示"系统配置缺少 RSA 公钥（SYSTEM_CONFIGS.rsaPublicKey）" | FR-011-005, NFR-011-002 | P1 |

## 3. RSA 加密校验

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 关联需求 | 优先级 |
|------|---------|---------|---------|---------|---------|--------|
| TC-011-011 | 验证登录请求中密码已加密 | 后端可达（或通过 Charles/Fiddler 代理拦截请求）；RSA 公钥已配置 | 1. 打开浏览器 DevTools Network 面板<br>2. 输入用户名密码并登录<br>3. 检查 POST /authcenter/login 请求 body | 1. body 格式为 `{"username":"admin","password":"<base64密文>"}`<br>2. password 值为非明文的长字符串（RSA 加密后的 base64）<br>3. 密文长度明显长于原始密码 | FR-011-004, NFR-011-002 | P1 |
| TC-011-012 | 加密失败不发送请求 | RSA 公钥格式非法（如随机字符串） | 1. 修改 `public/config.js` 的 rsaPublicKey 为非法 PEM<br>2. 输入有效用户名密码<br>3. 在 Network 面板观察 | 1. 对话框显示"密码加密失败，请检查 RSA 公钥配置"<br>2. Network 面板无 POST 请求发出<br>3. 登录流程终止 | FR-011-006 | P1 |

## 4. 登录状态持久化

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 关联需求 | 优先级 |
|------|---------|---------|---------|---------|---------|--------|
| TC-011-013 | localStorage 中不存储密码 | 已登录成功 | 1. 打开浏览器 DevTools Application → Local Storage<br>2. 查看 `dashboard-login-user` 的值 | 1. 该键存在<br>2. JSON 对象仅包含 `userId`, `userName`, `name` 三个字段<br>3. JSON 中不存在 `password`、`token`、`secret` 等敏感字段 | FR-011-011, FR-011-017, NFR-011-001 | P0 |
| TC-011-014 | localStorage 缓存损坏时视为未登录 | localStorage 中 `dashboard-login-user` 为非法 JSON 字符串 | 1. 手动设置 localStorage `dashboard-login-user` = `"broken-json{{{`<br>2. 刷新页面 | 1. 登录对话框出现<br>2. 无异常报错（静默降级） | FR-011-016 | P1 |
| TC-011-015 | localStorage 缓存缺少必需字段时视为未登录 | localStorage 中 `dashboard-login-user` = `{"userId":"1001"}`（缺少 userName） | 1. 手动设置 localStorage<br>2. 刷新页面 | 登录对话框出现 | FR-011-016 | P1 |
| TC-011-016 | localStorage 不可用时登录仍可完成 | 浏览器隐私模式或 localStorage 配额耗尽 | 1. 在隐私窗口中打开应用<br>2. 输入有效凭证登录 | 1. 登录成功，桌面正常渲染<br>2. 控制台可能有 `[auth-service] 登录信息缓存失败` 警告<br>3. 刷新页面后需要重新登录（会话无法持久化） | NFR-011-007 | P2 |

## 5. 登出

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 关联需求 | 优先级 |
|------|---------|---------|---------|---------|---------|--------|
| TC-011-017 | 通过状态栏登出 | 已登录，桌面正常渲染 | 1. 点击顶部状态栏的用户信息区域（显示"管理员"）<br>2. 在弹出的菜单或提示中点击"登出" | 1. localStorage 中 `dashboard-login-user` 被清除<br>2. localStorage 中 `dashboard-desktop-data` 被清除<br>3. 页面自动刷新（reload）<br>4. 刷新后登录对话框出现 | FR-011-018, FR-011-019, FR-011-020 | P0 |
| TC-011-018 | 登出后无法通过浏览器后退访问桌面 | 已登出，页面已刷新到登录界面 | 1. 点击浏览器"后退"按钮 | 仍在登录界面（页面经过 reload，历史栈重置） | FR-011-020 | P1 |
| TC-011-019 | 登出后主题偏好保留 | 已登录，用户设置了自定义主题 | 1. 登出<br>2. 页面刷新后观察登录界面 | 登录覆盖层背景色使用最近设置的主题（dashboard-theme 不随登出清除） | — | P2 |

## 6. 未登录防护

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 关联需求 | 优先级 |
|------|---------|---------|---------|---------|---------|--------|
| TC-011-020 | 未登录时桌面内容不可见 | 未登录，登录对话框显示中 | 1. 检查页面 DOM | 1. 全屏覆盖层（`.desktop-login-layer`）覆盖整个视口<br>2. 覆盖层内仅有登录对话框<br>3. 状态栏、视口、工具栏等桌面元素不在 DOM 中或不可见 | FR-011-021 | P0 |
| TC-011-021 | 登录对话框不可通过遮罩关闭 | 登录对话框显示中 | 1. 点击对话框外的遮罩区域<br>2. 按 ESC 键 | 1. 对话框不关闭<br>2. 不存在关闭按钮（×） | FR-011-022 | P1 |
| TC-011-022 | 已登录后状态栏显示用户信息 | 已登录为 admin/管理员 | 1. 查看顶部状态栏 | 1. 状态栏显示用户名"管理员"<br>2. 用户名可点击，点击后可登出 | FR-011-023 | P1 |

## 7. 后端失败降级 Mock

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 关联需求 | 优先级 |
|------|---------|---------|---------|---------|---------|--------|
| TC-011-023 | 后端返回 HTTP 500 时降级 | 后端 `/authcenter/login` 返回 500 Internal Server Error | 1. 配置代理指向返回 500 的后端<br>2. 使用 mock 有效凭证 `admin/admin123` 登录 | 1. 控制台有降级警告日志<br>2. 登录成功（通过 mock 降级路径）<br>3. 桌面正常渲染 | FR-011-008, NFR-011-005 | P1 |
| TC-011-024 | 后端返回 HTTP 403 时降级 | 后端 `/authcenter/login` 返回 403 Forbidden | 1. 配置代理指向返回 403 的后端<br>2. 使用 mock 有效凭证登录 | 1. 控制台有降级警告日志<br>2. 登录成功（通过 mock 降级路径） | FR-011-008 | P1 |
| TC-011-025 | 后端网络超时时降级 | 后端 `/authcenter/login` 无响应（连接超时） | 1. 配置代理指向不存在的地址或无响应的服务<br>2. 使用 mock 有效凭证登录 | 1. 等待网络超时<br>2. 控制台有降级警告日志<br>3. 登录成功（通过 mock 降级路径） | FR-011-008, NFR-011-005 | P1 |
| TC-011-026 | 后端可达但返回错误凭证 → 不降级到 mock | 后端 `/authcenter/login` 可达，返回 HTTP 401 且 body 含错误信息 | 1. 使用非 mock 凭证登录（不在 USERS_MOCK.json 中的用户名）<br>2. 观察行为 | 后端返回 401 → `loginViaBackend` 抛错 → catch 中降级到 mock → mock 也找不到 → 最终抛出"用户名或密码错误"。**注意**：此场景下后端已返回错误，但系统仍会二次尝试 mock（这是当前实现的行为，try/catch 不区分后端错误类型） | FR-011-008 [NEEDS CLARIFICATION: 后端返回 HTTP 401 时是否应直接暴露后端错误而非降级 mock？] | P2 |

## 8. 交互体验

| 编号 | 用例名称 | 前置条件 | 测试步骤 | 预期结果 | 关联需求 | 优先级 |
|------|---------|---------|---------|---------|---------|--------|
| TC-011-027 | 登录按钮加载状态 | 登录对话框中已填写表单 | 1. 点击"登录" | 1. 按钮显示旋转加载图标<br>2. 按钮文字变为"登录中…"<br>3. 用户名和密码输入框 disabled<br>4. 登录完成后恢复 | FR-011-003, NFR-011-008 | P1 |
| TC-011-028 | 登录期间防重复提交 | 登录进行中（后端响应慢） | 1. 点击"登录"<br>2. 在响应返回前再次点击（或快速多次点击） | 1. 第二次点击不触发新的登录请求（loading 状态阻止）<br>2. 只发出一次 POST/fetch 请求 | FR-011-003 | P1 |
| TC-011-029 | Enter 键提交登录 | 登录对话框中已填写表单 | 1. 在用户名输入框中按 Enter<br>2. 在密码输入框中按 Enter | 两种情况下均触发表单提交（登录） | NFR-011-010 | P1 |
| TC-011-030 | 密码可见性切换 | 登录对话框显示中，密码已输入 | 1. 点击密码输入框右侧的 👁 图标 | 1. 密码从圆点显示变为明文显示<br>2. 再次点击恢复圆点显示 | FR-011-001 | P2 |
| TC-011-031 | 错误提示语言 | 登录失败 | 1. 触发任意登录失败<br>2. 观察错误提示 | 错误提示为中文："用户名或密码错误"、"请输入用户名和密码"、"登录服务不可用，且模拟用户数据加载失败" 等，无英文错误码 | NFR-011-009 | P1 |
